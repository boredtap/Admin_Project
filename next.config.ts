// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  
  images: {
    domains: ['bt-coins.onrender.com', 'example.com'], // Add all domains your API uses for images
  },
};

module.exports = nextConfig;